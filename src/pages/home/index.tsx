import { Container, Header, Footer } from 'components/common';
import { Main } from 'components/pages/home';

const Home = () => {
	return (
		<Container>
			<Header />
			<Main />
			<Footer />
		</Container>
	);
};

export default Home;
