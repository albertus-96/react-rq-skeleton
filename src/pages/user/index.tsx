import { Container, Header, Footer } from 'components/common';
import { Main } from 'components/pages/user';

const User = () => {
	return (
		<Container>
			<Header />
			<Main />
			<Footer />
		</Container>
	);
};

export default User;
